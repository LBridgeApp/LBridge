/*
 * Как я понял, нет необходимости менять apollo.db,
 * даже таблицу uploadRecords.
 * Как я понял, в таблице uploadRecords хранится id последнего сахара,
 * который был загружен в libreview.
 * Все наши изменения производятся в sas.db.
 * Но apollo.db нужно удалять / создавать вместе с sas.db,
 * иначе будут конфликты.
 */